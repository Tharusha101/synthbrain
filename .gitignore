__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# Local Claude Code project context — kept on disk, not published.
CLAUDE.md

# Generated plots (keep the folder, ignore its contents except curated samples).
outputs/*
!outputs/.gitkeep
!outputs/samples/

# Cached datasets
.synthbrain/
*.npz
MNIST/

# ...but keep curated sample models (the trained best net) under version control.
!outputs/samples/*.npz
